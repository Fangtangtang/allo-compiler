# Passes for Streams

## `replace_stream_arrays`
Replace stream array accesses with accesses to concrete stream instances.
This includes constructing stream instances in global scope and replace the streams accessed in works' stream operations.